# Paths that must never exist in the public upstream/Nox repo's tree.
# Checked by scripts/check_public_safe.sh on every push to `upstream` and by
# CI on every push/PR to `main`. See PUBLIC_PRIVATE_SPLIT.md for the
# infra-vs-edge rationale behind this list.
#
# One path or glob fragment per line; matched as a substring against
# `git ls-tree -r --name-only`. Lines starting with # are comments.

execution/IBKRClient.cpp
execution/IBKRClient.hpp
execution/IBKR_MIGRATION.md

# Sports predictions/betting — undecided public-facing exposure as of
# 2026-07-12 (real Telegram bot, real odds-provider account, sports betting
# carries different reputational/legal considerations than trading infra).
# Kept private by default; revisit deliberately, don't let it sync by omission.
sports_predictions/
world_events/

# Full private codebase reference — documents the confidential edge (Skeptic
# workstreams, premium-selling research findings, IBKR path, sports/world-events
# pilots). The path-substring guard won't catch its *contents*, so it is
# denylisted explicitly. The public-facing overview lives in README.md + docs/.
CODEBASE.md
docs_private/
memory_bank.db
research/.cache/
