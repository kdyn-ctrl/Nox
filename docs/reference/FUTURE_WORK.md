# Future Work & Known Issues

## Critical Path (Do Soon)

### 1. **Resolve Half-Life Configuration Contradiction**
**Status**: Blocking  
**Issue**: 
- Commit de4bc87 (June 29) added env var loading to `HalfLifeDecay` constructor for dynamic configuration
- Later commit (f12443f, July 2) changed it to use default constants instead of env vars
- Result: Either env vars are now dead code, or the later change was incomplete

**Action**:
- [ ] Decide: Should HalfLifeDecay read from env vars OR use hardcoded defaults?
- [ ] If env vars: revert the July 2 change and ensure env var names are documented
- [ ] If hardcoded defaults: remove the env var reading code (de4bc87 is now orphaned)
- [ ] Add comments explaining the decision

**Why**: This affects system configurability. Operators need to know if they can tune decay rates at runtime.

---

### 2. **TradingView Code vs. Documentation Alignment**
**Status**: Medium urgency  
**Issue**:
- July 2 removed all TradingView references from docs (INDEX, USER_GUIDE, COMPLETE_GUIDE)
- Deprecated the TradingView integration guide
- But code might still contain TradingView-specific logic:
  - Webhook handler expecting TradingView payload format?
  - Import statements for TradingView libraries?
  - Signal validation logic specific to TradingView?

**Action**:
- [ ] Grep codebase for "tradingview", "TradingView", "webhook" to find remaining references
- [ ] For each match, decide: is it dead code (remove it) or necessary (document why)?
- [ ] If webhook handler is generic now, verify it doesn't assume TradingView JSON structure
- [ ] Update heartbeat/monitor.py comments to remove TradingView context
- [ ] Add a note to NOX_COMPLETE_GUIDE.md explaining what signals the webhook accepts now

**Why**: If someone tries to integrate a non-TradingView signal source, they need to know what the webhook actually expects.

---

### 3. **Consolidate Retry Logic**
**Status**: Medium urgency  
**Issue**:
- June 29 (de4bc87): Added HTTP session with retries in heartbeat/monitor.py
- July 2 (f12443f): Added generic `fetch_with_retry` and `call_with_retry` utilities
- Unclear if old retry patterns are being replaced or both exist (code duplication)

**Action**:
- [ ] Audit all HTTP calls in the codebase. Do they use the old pattern or new utilities?
- [ ] If both patterns exist, migrate everything to the new utilities
- [ ] Remove the old inline retry logic if it's been replaced
- [ ] Document the retry strategy (exponential backoff + jitter) in a central place
- [ ] Consider: should retries go in a dedicated module (heartbeat/retry_utils.py)?

**Why**: Duplication means bugs fixed in one place won't apply to the other. Also makes the codebase harder to reason about.

---

## Medium Priority (Clean Up Soon)

### 4. **Document china_positions.json**
**Status**: Unclear intent  
**Issue**:
- Added to .gitignore on July 2 as sensitive data
- No documentation about:
  - What it contains (position format?)
  - Where it's generated (which script?)
  - Why it's sensitive (personal trading data?)
  - How it's used (position reconciliation?)

**Action**:
- [ ] Add a comment in .gitignore explaining what this file is and why it's excluded
- [ ] Document the schema/format in FUTURE_WORK.md or a dedicated file
- [ ] Verify it's generated by the right script and at the right time
- [ ] Consider: does this belong in a data/ directory with a .gitkeep + .gitignore pattern?

**Why**: Future-you (and anyone else) won't know if this is critical state or a throwaway file.

---

### 5. **Reduce Refactor Commit Size**
**Status**: Process improvement  
**Issue**:
- Commit de4bc87 (June 29) is massive (~20+ file changes across signal generators, routers, position managers, docs, etc.)
- If something broke, isolating the root cause would be very hard

**Action**:
- [ ] For future large refactors, split into logical commits:
  - One for signal generator changes
  - One for router/order execution changes
  - One for position tracking changes
  - One for docs
- [ ] This makes `git bisect` and `git blame` far more useful
- [ ] Document the change strategy in a PR/commit message

**Why**: Smaller commits are easier to review, revert, and debug.

---

## Low Priority (Nice to Have)

### 6. **Create a Retry Strategy Document**
**Related to**: #3  
**Action**:
- [ ] Document the exponential backoff + jitter approach with examples
- [ ] Specify: what should retry (transient 429/5xx) vs. what shouldn't (4xx auth errors)
- [ ] Add to docs/DESIGN_THINKING.md or create docs/guides/ERROR_HANDLING.md

**Why**: Operators need to understand system resilience behavior.

---

### 7. **Audit Signal Generator Logic for Edge Cases**
**Related to**: #2 (webhook input assumptions)  
**Action**:
- [ ] OptionsSignalGenerator now has MACD alignment, volume metrics, breakout detection, LEAP support
- [ ] What happens if one of these indicators is unavailable (e.g., volume data missing)?
- [ ] Can the generator fall back, or does it block?
- [ ] Document the expected input format and error behavior

**Why**: Prevents silent failures when data sources lag or go down.

---

## Journal References
- [2026-07-02 TradingView Pivot](journal/2026_07_02_tradingview_pivot.md) — Detailed reflection on the week's work
